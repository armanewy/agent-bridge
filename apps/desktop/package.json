{
  "name": "@agentbridge/desktop",
  "version": "0.1.0",
  "description": "Capture tasks, send to agents, verify results.",
  "author": "AgentBridge",
  "private": true,
  "type": "module",
  "main": "dist-electron/main/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && vite build && tsc -p tsconfig.electron.json",
    "package": "electron-builder --dir --config electron-builder.yml",
    "dist": "electron-builder --win nsis --config electron-builder.yml",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit",
    "dev": "vite --host 127.0.0.1",
    "dev:electron": "pnpm build && electron ."
  },
  "dependencies": {
    "@agentbridge/core": "workspace:*",
    "@agentbridge/local-store": "workspace:*",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.17",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^33.3.0",
    "electron-builder": "^25.1.8",
    "vite": "^6.0.5"
  }
}
