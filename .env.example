# Copy this file to .env.local or set these values in your shell.
# .env.local is gitignored and is loaded by `pnpm dev:local`.

# Local development cloud endpoint used by the desktop app.
# `pnpm dev:local` chooses a free local port automatically when this is unset.
AGENTBRIDGE_CLOUD_URL=
