# Integration Notes

This file records cross-wave coordination notes. Agents should add notes here when a task needs a future wave to modify files outside its assigned area.

## Current Notes

- The repository started empty, so Wave 2 should create the root workspace scaffold.
- Scripts must quote paths because the workspace path contains a space.
- Native messaging manifest `allowed_origins` requires the final Chrome extension ID after loading/building the extension.
